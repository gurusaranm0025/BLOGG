/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  trailingSlash: true,
  // images: {
  //   unoptimized: true,
  // },
  // experimental: {
  //   serverActions: true,
  // },
};

module.exports = nextConfig;
